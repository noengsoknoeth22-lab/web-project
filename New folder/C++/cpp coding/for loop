#include <iostream>
#include <unistd.h> //for sleep function(1)

using namespace s    int main()
    while (true)
    {
        /* code */
    }
    
{
    // time : hh: mm: ss:

    int h = 0 m = 0 s = 0;
    cout << "=== digital clock===" << endl;

    for (;;)                // infinite loop
        cout << "\rtime: "; // \r updates the same line

    // 01 10 12 03
    if (h < 10 ~)
        cout << "0";
    {
        cout << h << ":";
    }
    if (m < 10)
        cout "0";
    {
        cout << m << ":";
    }
    {
        if (s < 10)
            cout << "0";
    }
    cout << s;

    cout.flush(); // don`t delay
    sleep(1);     // wiat for 1 second
    s++;
    if (s == 60)
    {
        s = 0;
        m++;
    }
    if (m == 60)
    {
        m = 0;
        h++;
    }
    if (h == 24)
    {
        h = o;
    }
    }

        return 0;
}